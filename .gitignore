node_modules/
dist/
src-tauri/target/
# Regenerated by tauri-build on every compile; editor schemas only.
src-tauri/gen/schemas/
.DS_Store
.env
.env.local
.claude/launch.json
# Working files from the Impeccable design plugin; per-machine, never source.
.impeccable/
# Wrangler's local-secrets file. Would hold a Cloudflare token if one ever
# existed; must never reach the public repo.
.dev.vars
