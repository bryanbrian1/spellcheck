{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the single app window. No plugins, no filesystem, no shell — the frontend only calls our own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
